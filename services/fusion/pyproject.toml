[project]
name = "sar-viewer-fusion"
version = "0.1.0"
description = "SAR-on-S2 fusion (4 modes + watermark) for sar-viewer."
requires-python = "==3.11.*"
dependencies = [
    "numpy>=1.26",
    "rasterio>=1.3",
    "rio-cogeo>=5.0",
    "pillow>=10.0",
    "matplotlib>=3.8",
    "pydantic>=2.7",
    "structlog>=24.1",
]

[tool.uv]
package = false
